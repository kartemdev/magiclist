export enum AuthEndPoints {
  LOGIN = '/auth/login',
  REGISTER = '/auth/register',
  REFRESH = '/auth/refresh',
  LOGOUT = '/auth/logout',
}

export enum UserEndPoints {
  USER_INFO = '/user/info',
  USER_VERIFIE = '/user/verifie',
  USER_SEND_VERIFIE = '/user/send-verifie',
  USER_CONFIRM_VERIFIE = '/user/confirm-verifie',
}
