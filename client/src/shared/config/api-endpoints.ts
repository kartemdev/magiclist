export enum AuthEndPoints {
  LOGIN = '/auth/login',
  REGISTER = '/auth/register',
  REFRESH = '/auth/refresh',
  LOGOUT = '/auth/logout',
}

export enum UserEndPoints {
  USER_INFO = '/user/info',
}
